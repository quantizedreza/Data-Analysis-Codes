Example: determining tha value of g using Monte-Carlo search. The experimental data is from https://github.com/vpagonis/CRCbook

In this experiment, a ball is dropped on Earth, t and v are measured. instead of determining g based on averaging and usual propogation of error, Monte Carlo is used to determine the parameter (g here). 
